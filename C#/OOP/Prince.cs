namespace OOP
{
    public class Prince
    {
        int.TryParse(Console.ReadLine(), out int testCases);

        while(testCeses-- > 0)
        {
            int crossing = 0;

            stringp[] point = Console.ReadLine().Split(' ', StringSplitOptions.RemoveEpmptyEntries);

            var start =  new Point(int.Parse(points[0]), int.Parse(points[1]));
            var end =  new Point(int.Parse(points[2]), int.Parse(points[3]));

            var prince = new Prince(start, end);

            int.TryParse(Console.ReadLine(), out int numOfPlanets);

            while(numOfPlanets-- > 0)
            {
                string[] planetParts = (int.Parse(planetParts[0]));

            
                var raduius = int.Parse(planetParts[2]);
            }
        }
    }
}